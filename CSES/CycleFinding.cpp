#include "bits/stdc++.h"

#ifdef DBG
#include "dbg.h"
#else
#define dbg(...)
#define dbgm(...)
#endif

using namespace std;
using i64 = int64_t;
#define all(x) begin(x), end(x)
#define rall(x) rbegin(x), rend(x)

int main () {
    ios_base::sync_with_stdio(false);
#ifndef LOCAL
    cin.tie(nullptr);
#endif



    return 0;
}
